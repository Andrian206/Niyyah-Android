<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Bagian tengah transparan -->
    <solid android:color="@android:color/transparent"/>
    <!-- Garis pinggir ungu -->
    <stroke
        android:width="2dp"
        android:color="@color/purple_dark"/> <!-- Gunakan warna ungu utama Anda -->
    <size
        android:width="12dp"
        android:height="12dp"/>
</shape>